# Theme Overrides

Custom MkDocs theme overrides for the SafeVixAI documentation site.

## Contents

HTML templates and CSS overrides that extend the default MkDocs material theme with:
- Custom navigation
- Brand styling
- Interactive API documentation widgets

## Usage

These overrides are referenced in `mkdocs.yml` via the `custom_dir` configuration option.
