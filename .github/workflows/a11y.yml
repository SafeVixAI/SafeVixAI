# SPDX-License-Identifier: MIT
# Copyright (c) 2026 SafeVixAI Team
name: Accessibility (a11y)

on:
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "mkdocs-requirements.txt"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pa11y:
    name: WCAG 2.1 AA Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 2

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v5.5.0
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: "mkdocs-requirements.txt"

      - name: Install MkDocs + dependencies
        run: pip install --require-hashes -r mkdocs-requirements.txt

      - name: Inject root docs
        run: |
          cp README.md docs/index.md
          cp SETUP.md docs/SETUP.md
          cp DESIGN.md docs/DESIGN.md
          cp AGENTS.md docs/AGENTS.md
          cp SKILL.md docs/SKILL.md

      - name: Build docs site
        run: mkdocs build --strict

      - name: Setup Node (for pa11y-ci)
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "22"


      - name: Serve site and run accessibility tests
        run: |
          cd site
          python -m http.server 8000 &
          SERVER_PID=$!
          cd ..

          # Wait for server
          for i in $(seq 1 10); do
            curl -s http://localhost:8000/ > /dev/null 2>&1 && break
            sleep 1
          done

          # Override config for CI
          cat > .pa11yci-ci.json << 'EOF'
          {
            "defaults": {
              "standard": "WCAG2AA",
              "timeout": 30000,
              "wait": 1500,
              "runner": "axe",
              "level": "error",
              "threshold": 10,
              "chromeLaunchConfig": {
                "args": ["--no-sandbox", "--disable-setuid-sandbox"]
              }
            },
            "urls": [
              "http://localhost:8000/",
              "http://localhost:8000/glossary/"
            ]
          }
          EOF

          npx --yes pa11y-ci@3.1.0 --config .pa11yci-ci.json 2>&1 | tee pa11y-output.txt
          EXIT_CODE=${PIPESTATUS[0]}

          kill $SERVER_PID 2>/dev/null
          exit $EXIT_CODE

      - name: Upload pa11y report on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: pa11y-report
          path: pa11y-output.txt
          retention-days: 30

  notify-failure:
    if: always() && (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled'))
    needs: [pa11y]
    uses: ./.github/workflows/notify-failure.yml
    with:
      workflow-name: 'Accessibility (a11y)'
      job-name: a11y
      run-id: ${{ github.run_id }}
      conclusion: failure
    secrets:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
