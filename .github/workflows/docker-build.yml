# SPDX-License-Identifier: MIT
# Copyright (c) 2026 SafeVixAI Team
name: Docker Build & Scan

on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]
    paths:
      - 'backend/Dockerfile'
      - 'chatbot_service/Dockerfile'
      - 'frontend/Dockerfile.frontend'
      - 'docker-compose.yml'
      - '.github/workflows/docker-build.yml'
  push:
    branches: [ "main" ]
    paths:
      - 'backend/Dockerfile'
      - 'chatbot_service/Dockerfile'
      - 'frontend/Dockerfile.frontend'
      - '.github/workflows/docker-build.yml'

permissions:
  contents: read
  security-events: write
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-commits:
    name: Validate Signed Commits
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4.2.2
        with:
          fetch-depth: 0
      - name: Verify HEAD commit is signed
        run: |
          COMMIT_SIGNATURE=$(git verify-commit HEAD 2>&1 || true)
          if echo "$COMMIT_SIGNATURE" | grep -q "gpg: Signature made"; then
            echo "✅ HEAD commit is signed"
          elif [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "⏭️ Skipping signed-commit check for pull_request trigger"
          else
            echo "❌ HEAD commit is NOT signed"
            exit 1
          fi

  build-and-scan:
    needs: [validate-commits]
    name: Build & Scan Images
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        include:
          - context: backend
            dockerfile: backend/Dockerfile
            image: svai-backend
          - context: chatbot_service
            dockerfile: chatbot_service/Dockerfile
            image: svai-chatbot
          - context: frontend
            dockerfile: frontend/Dockerfile.frontend
            image: svai-frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4.2.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3.10.0

      - name: Build Docker image
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8  # v6.15.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          tags: ${{ matrix.image }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner
        if: always()
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          image-ref: ${{ matrix.image }}
          format: 'sarif'
          output: 'trivy-${{ matrix.image }}.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
          ignore-unfixed: true

      - name: Upload Trivy scan results to GitHub
        if: always()
        uses: github/codeql-action/upload-sarif@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81  # v4.1.1
        continue-on-error: true
        with:
          sarif_file: 'trivy-${{ matrix.image }}.sarif'

      - name: Push to GHCR (main branch only) with versioned tags
        if: github.ref == 'refs/heads/main'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          SHA7="${{ github.sha }}" && SHA7="${SHA7:0:7}"
          DATE_TAG="$(date +'%Y%m%d')-${SHA7}"
          IMG="ghcr.io/${REPO_LOWER}/${{ matrix.image }}"
          docker tag ${{ matrix.image }} "${IMG}:latest"
          docker tag ${{ matrix.image }} "${IMG}:${SHA7}"
          docker tag ${{ matrix.image }} "${IMG}:${DATE_TAG}"
          docker push "${IMG}:latest"
          docker push "${IMG}:${SHA7}"
          docker push "${IMG}:${DATE_TAG}"
          # Keep only last 10 tags per image
          TAGS=$(docker images --filter=reference="${IMG}" --format "{{.Tag}}" | sort -r | tail -n +11)
          for TAG in $TAGS; do
            docker rmi "${IMG}:${TAG}" 2>/dev/null || true
          done

      - name: Install cosign
        if: github.ref == 'refs/heads/main'
        uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac  # v3.8.1

      - name: Sign image with cosign keyless
        if: github.ref == 'refs/heads/main'
        run: |
          REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMG="ghcr.io/${REPO}/${{ matrix.image }}"
          cosign sign --yes \
            -a "repo=${{ github.repository }}" \
            -a "workflow=${{ github.workflow }}" \
            -a "sha=${{ github.sha }}" \
            "${IMG}:latest"
          echo "✅ Signed ${IMG}:latest"

  notify-failure:
    if: always() && (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled'))
    needs: [build-and-scan]
    uses: ./.github/workflows/notify-failure.yml
    with:
      workflow-name: 'Docker Build & Scan'
      job-name: build-and-scan
      run-id: ${{ github.run_id }}
      conclusion: ${{ github.action_status || 'failure' }}
    secrets:
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
